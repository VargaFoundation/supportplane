{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "supportplane.fullname" . }}-postgres
  labels:
    {{- include "supportplane.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    {{- include "supportplane.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
{{- end }}
